name: Model Unittest GPU CE Release

on:
  schedule:
    - cron: "10 21 * * 0-4" # 每周日、一、二、三、四的6点
    - cron: "0 9 * * 5"
    - cron: "0 12 * * 5"
    - cron: "0 15 * * 5"
    - cron: "0 18 * * 5"
    - cron: "0 21 * * 5"
    - cron: "0 23 * * 5"
  workflow_dispatch:
    inputs:
      update_baseline_models:
        description: 'Update baseline'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'all'
          - 'llama'
          - 'qwen3'
          - 'glm_moe'
          - 'qwen3_next'
          - 'qwen3moe'
          - 'phi3'
          - 'deepseek_v3'
          - 'qwen2_moe'
          - 'qwen2'
          - 'gemma3_text'
          - 'paddleocr_vl'
      FLAGS_enable_CE:
        required: false
        default: 'CE_Release_cu129_py312_nightly'
        type: choice
        options:
          - 'CE_Release_cu129_py312_nightly'
          - 'CE_Release_cu132_py312'
          - 'CE_Release_cu130_py313'
          - 'CE_Release_cu130_py312'
          - 'CE_Release_cu130_py311'
          - 'CE_Release_cu129_py313'
          - 'CE_Release_cu129_py312_weekly'

concurrency:
  group: model-unittest-${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

env:
  PR_ID: ${{ github.event.pull_request.number || '0' }}
  COMMIT_ID: ${{ github.event.pull_request.head.sha || github.sha }}
  BRANCH: ${{ github.event.pull_request.base.ref || github.ref_name }}
  work_dir: ${{ github.workspace }}
  CE_JOB_NAME: model-unittest-gpu-ce-release
  TASK: PaddleFormers_CE_model_unittest_release_${{ github.event.pull_request.number || github.run_id }}
  no_proxy: "localhost,bj.bcebos.com,su.bcebos.com,bcebos.com,apiin.im.baidu.com,gitee.com,aliyun.com,.baidu.com,.tuna.tsinghua.edu.cn"

defaults:
  run:
    shell: bash

jobs:
  check_skip:
    name: Check skip-models-ce label
    runs-on: 'ernie-cpu'

    outputs:
      skip: ${{ steps.check_skip.outputs.skip }}

    steps:
      - name: Check skip-models-ce label
        id: check_skip
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            labels='${{ toJson(github.event.pull_request.labels.*.name) }}'
            echo "PR labels: $labels"
            if echo "$labels" | grep -q "skip-models-ce"; then
              echo "skip=true" >> "$GITHUB_OUTPUT"
            else
              echo "skip=false" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Skip CE but mark success
        if: steps.check_skip.outputs.skip == 'true'
        run: |
          echo "skip-models-ce label found"
          echo "Downstream GPU jobs will be skipped"

  model-unittest-gpu-ce-release:
    needs: check_skip
    if: needs.check_skip.outputs.skip == 'false'
    name: model-unittest-gpu-ce-release
    runs-on: 'models'
    steps:
      - name: Determine CE branch by schedule time
        id: determine_flag
        run: |
          schedule_time="${{ github.event.schedule || '' }}"
          if [[ "$schedule_time" == "10 21 * * 0-4" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu129_py312_nightly" >> $GITHUB_OUTPUT
          elif [[ "$schedule_time" == "0 9 * * 5" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu130_py313" >> $GITHUB_OUTPUT
          elif [[ "$schedule_time" == "0 12 * * 5" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu130_py312" >> $GITHUB_OUTPUT
          elif [[ "$schedule_time" == "0 15 * * 5" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu130_py311" >> $GITHUB_OUTPUT
          elif [[ "$schedule_time" == "0 18 * * 5" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu129_py313" >> $GITHUB_OUTPUT
          elif [[ "$schedule_time" == "0 21 * * 5" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu129_py312_weekly" >> $GITHUB_OUTPUT
          elif [[ "$schedule_time" == "0 23 * * 5" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu132_py312" >> $GITHUB_OUTPUT
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "FLAGS_enable_CE=${{ github.event.inputs.FLAGS_enable_CE }}" >> $GITHUB_OUTPUT
          else
            echo "FLAGS_enable_CE=CE_Release_cu129_py312_nightly" >> $GITHUB_OUTPUT
          fi
      - name: Determine Image Name
        env:
          FLAGS_enable_CE: ${{ steps.determine_flag.outputs.FLAGS_enable_CE }}
        run: |
          if [[ "${FLAGS_enable_CE}" == *cu132* ]]; then
            echo "IMAGE_NAME=ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddle:ubuntu24-cuda132-py312-dev" >> "$GITHUB_ENV"
          elif [[ "${FLAGS_enable_CE}" == *cu130* ]]; then
            echo "IMAGE_NAME=ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddle:cuda130-dev" >> "$GITHUB_ENV"
          elif [[ "${FLAGS_enable_CE}" == *cu129* ]]; then
            echo "IMAGE_NAME=ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddle:cuda129-dev" >> "$GITHUB_ENV"
          elif [[ "${FLAGS_enable_CE}" == *cu126* ]]; then
            echo "IMAGE_NAME=ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddle:cuda126-dev" >> "$GITHUB_ENV"
          else
            echo "IMAGE_NAME=ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddle:cuda126-dev" >> "$GITHUB_ENV"
          fi
      - name: Run Container
        env:
          FLAGS_dynamic_static_unified_comm: "True"
          PIP_CACHE_DIR: /root/.cache/pip
        run: |
          work_dir="${{ github.workspace }}"
          container_name=${TASK}-$(date +%Y%m%d-%H%M%S)
          echo "container_name=${container_name}" >> "$GITHUB_ENV"
          DOCKER_VER=$(docker version --format '{{.Server.Version}}' | cut -d. -f1,2)
          if (( $(echo "$DOCKER_VER < 19.03" | bc -l) )); then
            GPU_OPTION="--runtime=nvidia"
          else
            GPU_OPTION="--gpus all"
          fi
          echo "DOCKER_VER=${DOCKER_VER}"
          echo "GPU_OPTION=${GPU_OPTION}"
          docker run -d -t ${GPU_OPTION} --name ${container_name} --net=host -v /dev/shm:/dev/shm --shm-size=32G \
            -v ${work_dir}/../../..:${work_dir}/../../.. \
            -v ${work_dir}:/workspace \
            -v /home/.cache/:/root/.cache/ \
            -v /home/paddle-1/models/:/home/models/ \
            -e "work_dir=$work_dir" \
            -e "PR_ID=$PR_ID" \
            -e "BRANCH=$BRANCH" \
            -e "COMMIT_ID=$COMMIT_ID" \
            -e "no_proxy=$no_proxy" \
            -e "CE_JOB_NAME=$CE_JOB_NAME" \
            -e "FLAGS_dynamic_static_unified_comm=$FLAGS_dynamic_static_unified_comm" \
            -e HF_PROXY_PATH=${work_dir}/../../../proxy_huggingface \
            -e AISTUDIO_PROXY_PATH=${work_dir}/../../../proxy_aistudio \
            -e PF_HOME=/home/models/ \
            -e PIP_CACHE_DIR \
            -w /workspace --privileged ${IMAGE_NAME}
      - name: Download Code
        run: |
          work_dir="${{ github.workspace }}"
          docker exec -i -e work_dir="$work_dir" -e PR_ID="$PR_ID" -e BRANCH="$BRANCH" $container_name /bin/bash <<-DOCKER_EOF
          rm -rf * .[^.]*
          # echo "Downloading PaddleFormers.tar"
          # wget -q --no-proxy  https://paddle-qa.bj.bcebos.com/CodeSync/develop/PaddleFormers.tar --no-check-certificate
          # echo "Extracting PaddleFormers.tar"
          # rm -rf PaddleFormers
          # tar xf PaddleFormers.tar && rm -rf PaddleFormers.tar

          echo "work_dir = ${work_dir}"
          source ${work_dir}/../../../proxy_huggingface
          git clone -b release/1.2 --depth=20 https://github.com/PaddlePaddle/PaddleFormers.git
          cd PaddleFormers
          git config --global user.name "PaddleCI"
          git config --global user.email "paddle_ci@example.com"
          git pull
          git submodule update --init --recursive --force
          if [ -n "$PR_ID" ] && [ "$PR_ID" != "0" ]; then
            git fetch origin pull/${PR_ID}/head
            git checkout -b PR_${PR_ID} FETCH_HEAD
            git remote add upstream https://github.com/PaddlePaddle/PaddleFormers.git
            echo "Checking out ${BRANCH}..."
            git fetch upstream ${BRANCH}:${BRANCH}
            git merge ${BRANCH} --no-edit
            git diff --numstat ${BRANCH} -- | awk "{print \$NF}"
          else
            echo "Not in a pull_request event. Skipping PR-specific operations."
          fi
          git log --pretty=oneline -10
          DOCKER_EOF
      - name: Determine trigger mode
        id: trigger_mode
        env:
          PR_BODY: ${{ github.event.pull_request.body || '' }}
        run: |
          UPDATE_BASELINE_MODELS="false"
          MODE="pr"
          PR_NUMBER="${PR_ID}"
          
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            MODE="manual"
            UPDATE_BASELINE_MODELS="${{ github.event.inputs.update_baseline_models }}"
            PR_NUMBER=0
          elif [[ "${{ github.event_name }}" == "schedule" ]]; then
            MODE="schedule"
            UPDATE_BASELINE_MODELS="false"
            PR_NUMBER=0
          elif [[ "${{ github.event_name }}" == "pull_request" ]]; then
            # Parse PR description for [update-baseline: models]
            if grep -qE '\[update-baseline:[[:space:]]*[a-zA-Z0-9_,]+\]' <<< "$PR_BODY"; then
              UPDATE_BASELINE_MODELS=$(grep -oE '\[update-baseline:[[:space:]]*([a-zA-Z0-9_,]+)\]' <<< "$PR_BODY" | sed 's/.*\[\(.*\)\].*/\1/' | sed 's/update-baseline:[[:space:]]*//g')
              MODE="pr"
              echo "Detected baseline update from PR description: $UPDATE_BASELINE_MODELS"
            fi
          fi
          
          echo "mode=$MODE" >> "$GITHUB_OUTPUT"
          echo "update_baseline_models=$UPDATE_BASELINE_MODELS" >> "$GITHUB_OUTPUT"
          echo "pr_number=$PR_NUMBER" >> "$GITHUB_OUTPUT"
      - name: Change python version
        env:
          FLAGS_enable_CE: ${{ steps.determine_flag.outputs.FLAGS_enable_CE }}
        run: |
          python_version=$(echo "${FLAGS_enable_CE##*_py}" | sed 's/_.*$//;s/\(.\)\(..\)/\1.\2/')
          docker exec -i -e "work_dir=$work_dir" -e "python_version=$python_version" $container_name /bin/bash <<-DOCKER_EOF
          echo "Change python version to $python_version"
          set -e
          cp ${work_dir}/../../../Miniconda3-latest-Linux-x86_64.sh /tmp/miniconda.sh
          bash /tmp/miniconda.sh -b -p /opt/conda
          export PATH=/opt/conda/bin:$PATH
          source ${work_dir}/../../../proxy_huggingface
          conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main
          conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/r
          conda create -n py_$python_version python=$python_version -y
          . /opt/conda/etc/profile.d/conda.sh
          conda activate py_$python_version
          python --version
          DOCKER_EOF
      - name: Test
        id: test
        env:
          FLAGS_enable_CE: ${{ steps.determine_flag.outputs.FLAGS_enable_CE }}
        run: |
          python_version=$(echo "${FLAGS_enable_CE##*_py}" | sed 's/_.*$//;s/\(.\)\(..\)/\1.\2/')
          # Use step outputs from previous step instead of env vars
          MODE="${{ steps.trigger_mode.outputs.mode }}"
          UPDATE_BASELINE_MODELS="${{ steps.trigger_mode.outputs.update_baseline_models }}"
          PR_NUMBER="${{ steps.trigger_mode.outputs.pr_number }}"
          BRANCH="${{ github.event.pull_request.base.ref || github.ref_name }}"
          work_dir="${{ github.workspace }}"
          docker exec -i \
            -e python_version="$python_version" \
            -e FLAGS_enable_CE="$FLAGS_enable_CE" \
            -e work_dir="$work_dir" \
            -e MODE="$MODE" \
            -e UPDATE_BASELINE_MODELS="$UPDATE_BASELINE_MODELS" \
            -e PR_NUMBER="$PR_NUMBER" \
            -e BRANCH="$BRANCH" \
            -e GITHUB_REPOSITORY="$GITHUB_REPOSITORY" \
            $container_name /bin/bash <<-DOCKER_EOF
          . /opt/conda/etc/profile.d/conda.sh
          conda activate py_$python_version
          python --version
          ldconfig
          mkdir -p /root/.cache/pip
          pip cache dir
          set -e
          rm -rf /root/.cache/aistudio/
          cd /workspace/PaddleFormers && git config --global --add safe.directory $PWD
          echo "=== Trigger Mode: ${MODE} ==="
          echo "Update baseline: ${UPDATE_BASELINE_MODELS}"
          echo "BRANCH: ${BRANCH}"
          echo "PR number: ${PR_NUMBER}"
          source ${work_dir}/../../../proxy_huggingface
          bash -x scripts/regression/ci_model_unittest.sh False $FLAGS_enable_CE "$UPDATE_BASELINE_MODELS" $BRANCH ${PR_NUMBER}
          DOCKER_EOF
      - name: Display Test Summary
        if: always()
        run: |
          file_success=${{ github.workspace }}/PaddleFormers/model_unittest_logs/model_unittest.log
          file_failed=${{ github.workspace }}/PaddleFormers/model_unittest_logs/model_unittest_FAIL.log
          if [[ "${{ steps.test.outcome }}" == "success" ]]; then
            echo "✅ Test passed successfully"
            tail -n 1 ${file_success} || true
          else
            echo "❌ Test failed with status: ${{ steps.test.outcome }}"
            grep "^FAILED scripts/regression" ${file_failed} | awk '{print $2}' | sort -u || true
          fi
      - name: Upload Products
        if: always()
        env:
          home_path: ${{ github.workspace }}/../../..
          bos_file: ${{ github.workspace }}/../../../bos/BosClient.py
          java_file: ${{ github.workspace }}/../../../jdk1.8.0_351
          allure_file: ${{ github.workspace }}/../../../allure-2.19.0/bin/allure
          FLAGS_enable_CE: ${{ steps.determine_flag.outputs.FLAGS_enable_CE }}
          MODE: ${{ steps.trigger_mode.outputs.mode }}
          PR_NUMBER: ${{ steps.trigger_mode.outputs.pr_number }}
          RUN_ID: ${{ github.run_id }}
        run: |
          docker exec -t -e FLAGS_enable_CE="$FLAGS_enable_CE" $container_name /bin/bash -c '
          source ${work_dir}/../../../proxy_huggingface
          python -m pip install bce-python-sdk==0.8.74 -i https://pypi.org/simple
          if [ ! -f "${{ env.bos_file }}" ]; then
            wget -q --no-proxy -O ${{ env.home_path }}/bos_new.tar.gz https://xly-devops.bj.bcebos.com/home/bos_new.tar.gz --no-check-certificate
            mkdir ${{ env.home_path }}/bos
            tar xf ${{ env.home_path }}/bos_new.tar.gz -C ${{ env.home_path }}/bos
          fi
          if [ ! -f "${{ env.java_file }}" ]; then
            wget -q --no-proxy -O ${{ env.home_path }}/java_linux.tar.gz https://paddle-qa.bj.bcebos.com/java/java_linux.tar.gz --no-check-certificate
            tar xf ${{ env.home_path }}/java_linux.tar.gz
          fi
          if [ ! -f "${{ env.allure_file }}" ]; then
            wget -q --no-proxy -O ${{ env.home_path }}/allure-2.19.0.zip https://xly-devops.bj.bcebos.com/tools/allure-2.19.0.zip --no-check-certificate
            unzip -q ${{ env.home_path }}/allure-2.19.0.zip
          fi
          
          MODE=${{ env.MODE }}
          if [[ "$MODE" == "pr" ]]; then
            bos_prefix="${PR_ID}/${COMMIT_ID}"
          else
            bos_prefix="${MODE}_${FLAGS_enable_CE}/${{ env.RUN_ID }}"
          fi

          # logs
          cd /workspace/PaddleFormers/model_unittest_logs
          for FILE in /workspace/PaddleFormers/model_unittest_logs/*; do
            file=$(basename "$FILE")
            python ${{ env.bos_file }} $file paddle-github-action/PR/PaddleFormers/ce-model-unittest-gpu/${bos_prefix}/logs
            exit_code=$?
            if [ $exit_code -eq 0 ]; then
              echo "$file: https://paddle-github-action.bj.bcebos.com/PR/PaddleFormers/ce-model-unittest-gpu/${bos_prefix}/logs/$file"
            else
              echo "ERROR: Failed to upload $file (exit code: $exit_code)"
            fi
          done
          # allure
          cd /workspace/PaddleFormers/
          export JAVA_HOME="${{ env.java_file }}"
          export JRE_HOME="${JAVA_HOME}/jre"
          export CLASSPATH="${JAVA_HOME}/lib"
          export PATH="${JAVA_HOME}/bin:${PATH}"
          ${{ env.allure_file }} generate result -o report
          tar -czf report.tar.gz report
          python ${{ env.bos_file }} report.tar.gz paddle-github-action/PR/PaddleFormers/ce-model-unittest-gpu/${bos_prefix}/logs
          exit_code=$?
          if [ $exit_code -eq 0 ]; then
            echo "report: https://paddle-github-action.bj.bcebos.com/PR/PaddleFormers/ce-model-unittest-gpu/${bos_prefix}/logs/report.tar.gz"
          else
            echo "ERROR: Failed to upload report.tar.gz (exit code: $exit_code)"
          fi
          '
      - name: Terminate And Delete the Container
        if: always()
        run: |
          docker rm -f $container_name 2>/dev/null || true
          
  upload-allure:
    name: upload-allure
    needs: [model-unittest-gpu-ce-release]
    if: always()
    runs-on: 'ernie-cpu'
    permissions:
      contents: write
      pages: write
      id-token: write
    steps:
      - name: Checkout gh-pages
        uses: actions/checkout@v5
        with:
          ref: gh-pages
          path: gh-pages
        continue-on-error: true
      - name: Determine CE branch by schedule time
        id: determine_flag
        run: |
          schedule_time="${{ github.event.schedule || '' }}"
          if [[ "$schedule_time" == "10 21 * * 0-4" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu129_py312_nightly" >> $GITHUB_OUTPUT
          elif [[ "$schedule_time" == "0 9 * * 5" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu130_py313" >> $GITHUB_OUTPUT
          elif [[ "$schedule_time" == "0 12 * * 5" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu130_py312" >> $GITHUB_OUTPUT
          elif [[ "$schedule_time" == "0 15 * * 5" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu130_py311" >> $GITHUB_OUTPUT
          elif [[ "$schedule_time" == "0 18 * * 5" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu129_py313" >> $GITHUB_OUTPUT
          elif [[ "$schedule_time" == "0 21 * * 5" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu129_py312_weekly" >> $GITHUB_OUTPUT
          elif [[ "$schedule_time" == "0 23 * * 5" ]]; then
            echo "FLAGS_enable_CE=CE_Release_cu132_py312" >> $GITHUB_OUTPUT
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "FLAGS_enable_CE=${{ github.event.inputs.FLAGS_enable_CE }}" >> $GITHUB_OUTPUT
          else
            echo "FLAGS_enable_CE=CE_Release_cu129_py312_nightly" >> $GITHUB_OUTPUT
          fi
      - name: Determine trigger mode && Restore history
        id: trigger_mode
        env:
          FLAGS_enable_CE: ${{ steps.determine_flag.outputs.FLAGS_enable_CE }}
        run: |
          MODE="pr"
          PR_NUMBER="${PR_ID}"
          mkdir -p result
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            MODE="manual"
            PR_NUMBER=0
            cp -r gh-pages/model_$FLAGS_enable_CE/manual/history result/ || true
            destination_dir="model_$FLAGS_enable_CE/manual"
          elif [[ "${{ github.event_name }}" == "schedule" ]]; then
            MODE="schedule"
            PR_NUMBER=0
            cp -r gh-pages/model_$FLAGS_enable_CE/schedule/history result/ || true
            destination_dir="model_$FLAGS_enable_CE/schedule"
          elif [[ "${{ github.event_name }}" == "pull_request" ]]; then
            MODE="pr"
            PR_NUMBER="${PR_ID}"
            cp -r gh-pages/model_$FLAGS_enable_CE/$PR_NUMBER/history result/ || true
            destination_dir="model_$FLAGS_enable_CE/$PR_NUMBER"
          fi
          
          echo "mode=$MODE" >> "$GITHUB_OUTPUT"
          echo "pr_number=$PR_NUMBER" >> "$GITHUB_OUTPUT"
          echo "destination_dir=$destination_dir" >> "$GITHUB_OUTPUT"
      - name: Download report
        id: download_report
        env:
          MODE: ${{ steps.trigger_mode.outputs.mode }}
          RUN_ID: ${{ github.run_id }}
          FLAGS_enable_CE: ${{ steps.determine_flag.outputs.FLAGS_enable_CE }}
        run: |
          MODE=${{ env.MODE }}
          if [[ "$MODE" == "pr" ]]; then
            bos_prefix="${PR_ID}/${COMMIT_ID}"
          else
            bos_prefix="${MODE}_${FLAGS_enable_CE}/${{ env.RUN_ID }}"
          fi
          echo "report: https://paddle-github-action.bj.bcebos.com/PR/PaddleFormers/ce-model-unittest-gpu/${bos_prefix}/logs/report.tar.gz"
          wget -q --no-proxy \
            https://paddle-github-action.bj.bcebos.com/PR/PaddleFormers/ce-model-unittest-gpu/${bos_prefix}/logs/report.tar.gz \
            --no-check-certificate
          tar -xzf report.tar.gz
          if [ -d "report" ]; then
            echo "report directory exists"
            rm -rf report/data/attachments/*
          else
            echo "ERROR: report directory not found!"
            exit 1
          fi
      - name: Deploy to GitHub Pages
        if: always()
        timeout-minutes: 5
        continue-on-error: true
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./report
          destination_dir: ${{ steps.trigger_mode.outputs.destination_dir }}
      - name: Display GitHub Pages URL
        if: always()
        env:
          FLAGS_enable_CE: ${{ steps.determine_flag.outputs.FLAGS_enable_CE }}
          MODE: ${{ steps.trigger_mode.outputs.mode }}
        run: |
          # GitHub Pages URL
          DEST_DIR="${{ steps.trigger_mode.outputs.destination_dir }}"
          PAGES_URL="https://paddlepaddle.github.io/PaddleFormers/${DEST_DIR}/"
          echo "GitHub Allure Pages URL: ${PAGES_URL}"

          echo "## 🚀 Allure Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Item | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| Workflow | ${{ github.workflow }} |" >> $GITHUB_STEP_SUMMARY
          echo "| CE Mode | $MODE_$FLAGS_enable_CE |" >> $GITHUB_STEP_SUMMARY
          echo "| Time | $(date +%Y%m%d) |" >> $GITHUB_STEP_SUMMARY
          echo "| Report | [Open Report](${PAGES_URL}) |" >> $GITHUB_STEP_SUMMARY